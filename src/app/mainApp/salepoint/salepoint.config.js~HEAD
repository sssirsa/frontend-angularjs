(function () {
    angular
        .module('app.mainApp.salepoint')
        .config(salepointConfig);
    function salepointConfig($translatePartialLoaderProvider){
        $translatePartialLoaderProvider.addPart('app/mainApp/salepoint');
    }
}) ();
